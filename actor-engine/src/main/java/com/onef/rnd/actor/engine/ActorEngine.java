package com.onef.rnd.actor.engine;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class ActorEngine {
	private final Logger log = LoggerFactory.getLogger(getClass());

	public void start() {
		log.debug("actor engine started");
	}
}
